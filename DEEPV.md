## DeepV Code Added Memories
- 用户要求所有回复必须使用中文。
- 用户要求给出的所有命令必须经过源代码验证，确认是可用的有效指令，不能猜测或假设。
- 通用MCP触发原则：当用户表达的意图明确对应某个已加载的MCP服务时，必须立即调用对应工具，不要犹豫、不要等待更多信息、不要反复确认。可选参数应使用默认值直接触发。已加载的MCP服务及其触发场景如下：
- SSH MCP（远程服务器管理）：用户提到查看服务器状态/系统情况、CPU/内存/磁盘/网络信息、进程管理、日志查看、服务启停、文件读写、执行远程命令、安全检查、负载诊断等，立即调用对应SSH MCP工具，host_name为可选参数直接使用默认主机。
- Redis MCP（数据存储/缓存/消息）：用户提到存储数据、读取缓存、设置过期、操作哈希/列表/集合/有序集合/流、发布订阅消息、JSON文档操作、向量相似度搜索、查看Redis信息/索引等，立即调用对应Redis MCP工具。
